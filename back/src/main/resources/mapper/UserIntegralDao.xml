<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserIntegralDao">

    <select id="selectByYonghuId" resultType="com.entity.UserIntegralEntity">
        SELECT id, yonghu_id as yonghuId, integral, source, source_type as sourceType,
               ref_id as refId, create_time as createTime
        FROM user_integral
        WHERE yonghu_id = #{yonghuId}
        ORDER BY create_time DESC
    </select>

    <select id="sumIntegralByYonghuId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(integral), 0)
        FROM user_integral
        WHERE yonghu_id = #{yonghuId}
    </select>

</mapper>
